TODO:
1) secure ssh: https://github.com/vitalk/ansible-secure-ssh
2) add support for altering config.txt via options provided in api/config file
3) refactor default options (api, default key, etc) into a env vars/external file to make them easy to alter
4) build initramfs image and put in on a CDN to be downloadable
