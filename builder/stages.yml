---
- hosts: buildernodes
  remote_user: pi
  become: yes
  become_method: sudo
  tasks:
    - name: sentinel start
      command: echo start
    - import_tasks: update_software.yml
    - name: sentinel done
      command: echo done